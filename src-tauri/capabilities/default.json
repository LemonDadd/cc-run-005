{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "KidMath 默认能力：仅 Tauri 核心 IPC 与窗口能力。不启用 http/fs/net 等任何权限，确保纯离线。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize"
  ]
}
